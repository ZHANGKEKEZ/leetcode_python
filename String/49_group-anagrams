class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        d={}
        for s in strs:
            key="".join(sorted(s))#键值怎么选是关键
            d[key]=d.get(key,[])+[s]
        res=[]
        for value in d.values():
            res.append(value)
        return res
